const $ = selector => document.querySelector(selector);
let csrf = '';
const escapeHtml = value => String(value ?? '').replace(/[&<>"']/g, char => ({ '&': '&amp;', '<': '&lt;', '>': '&gt;', '"': '&quot;', "'": '&#39;' })[char]);
async function api(url, options = {}) { const response = await fetch(url, { headers: { 'Content-Type': 'application/json', ...(csrf ? { 'X-CSRF-Token': csrf } : {}), ...(options.headers || {}) }, ...options }); const data = await response.json().catch(() => ({})); if (!response.ok) throw new Error(data.error || 'Não foi possível concluir.'); return data; }
function showApp() { $('#loginView').hidden = true; $('#appView').hidden = false; $('#cpf').focus(); }
async function boot() { try { const session = await api('/api/staff/session'); csrf = session.csrf; showApp(); } catch { $('#loginView').hidden = false; } }
$('#loginForm').addEventListener('submit', async event => { event.preventDefault(); try { const session = await api('/api/staff/login', { method: 'POST', body: JSON.stringify({ username: $('#username').value.trim(), password: $('#password').value }) }); csrf = session.csrf; showApp(); } catch (error) { $('#loginStatus').textContent = error.message; } });
$('#lookupForm').addEventListener('submit', async event => { event.preventDefault(); const result = $('#result'); result.hidden = false; result.className = 'result'; result.textContent = 'Consultando cadastro…'; try { const data = await api('/api/staff/cpf-lookup', { method: 'POST', body: JSON.stringify({ cpf: $('#cpf').value }) }); if (!data.found) { result.className = 'result not-found'; result.innerHTML = '<p class="kicker">NENHUM CADASTRO ATIVO</p><h2>Cliente não encontrado</h2><p>Não há uma compra registrada para este CPF.</p>'; return; } const formatDate = value => new Date(value).toLocaleDateString('pt-BR'); result.innerHTML = `<p class="kicker">CLIENTE LOCALIZADO</p><h2>${escapeHtml(data.customer)}</h2><span class="pill">BENEFÍCIO ${escapeHtml(data.status).toUpperCase()}</span><div class="result-grid"><div class="data"><small>CPF CONFERIDO</small><strong>Final ${escapeHtml(data.cpfLastFive)}</strong></div><div class="data"><small>PLANO</small><strong>${escapeHtml(data.plan)}</strong></div><div class="data"><small>VÁLIDO ATÉ</small><strong>${formatDate(data.validUntil)}</strong></div></div>`; } catch (error) { result.className = 'result not-found'; result.textContent = error.message; } });
$('#logout').addEventListener('click', async () => { await api('/api/staff/logout', { method: 'POST' }).catch(() => {}); csrf = ''; $('#appView').hidden = true; $('#loginView').hidden = false; $('#password').value = ''; });
boot();
